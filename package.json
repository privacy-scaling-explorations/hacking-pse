{
  "name": "hacking-pse",
  "version": "0.1.0",
  "description": "Hacking PSE Hackathon",
  "repository": "https://github.com/privacy-scaling-explorations/hacking-pse",
  "license": "MIT",
  "author": "PSE",
  "scripts": {
    "build": "pnpm --recursive --sort run build",
    "dev:server": "pnpm --filter backend run dev:optimism-sepolia",
    "start:interface": "pnpm --filter interface run start",
    "dev:interface": "pnpm --filter interface run dev"
  },
  "engines": {
    "node": "20",
    "pnpm": "9"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}
